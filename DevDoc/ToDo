ToDo issues
===========================
3, INTERFACE_ADDREF/ARRAY_ADDREF/INTERFACE_RELEASE/ARRAY_RELEASE 应该统一为一个，名字可以为：EL_ADDREF/EL_RELEASE.
4, SEARCH_SERVICE, DOWNLOAD_SERVICE, THROTTLE_SERVICE, ACCOUNT_SERVICE, NFC_SERVICE, TELEPHONY_SERVICE 没有实现。
7, 调整继承结构,实现代码复用
8, ImageView: SetImageDrawable -> SetDrawable
10, pthread_key_create(&key, threadDestructor)
void XXX::threadDestructor(void *st)
{
    XXX* const self = static_cast<XXX*>(st);
    self->Release();
}

11, 在AndroidManifest.xml里支持namespace，如 <activity android:name="Elastos.FrameworkTest.App.CPendingIntentStubActivity"
    android:label="CPendingIntentStubActivity"/>

12, Object : public IObject, public ISynchronize, public IWeakReferenceSource, public ElRefBase {}
    CObject : public Object {}

13, eco头文件优化, 将类的静态方法定义从.h文件中转移到.cpp文件中, 如Elastos.Droid.Core.h