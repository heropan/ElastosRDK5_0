TARGET_NAME = fwLocalSocket
TARGET_TYPE = eco

SOURCES  = fwLocalSocket.car
SOURCES += CActivityOne.cpp
SOURCES += Elastos.Droid.Core.cpp


ifneq "$(TEST_TYPE)" ""
C_DEFINES:= $(C_DEFINES) -DAutoExit
endif

ELASTOS_LIBS  = $(XDK_USER_LIB)/Elastos.Runtime.lib
ELASTOS_LIBS += $(XDK_USER_LIB)/Elastos.Droid.lib
ELASTOS_LIBS += $(XDK_TARGETS)/Elastos.Runtime.eco
ELASTOS_LIBS += $(XDK_TARGETS)/Elastos.CoreLibrary.eco
ELASTOS_LIBS += $(XDK_USER_LIB)/crt0.lib
ELASTOS_LIBS += $(XDK_USER_LIB)/tstcom.lib
LIBRARIES += -lm -licuuc -llog
LIBRARIES += -licuuc -lcutils -lutils

APPPACK = 1
DIRRESOURCES += $(MAKEDIR)/res;
DIRRESOURCES += $(MAKEDIR)/data;
