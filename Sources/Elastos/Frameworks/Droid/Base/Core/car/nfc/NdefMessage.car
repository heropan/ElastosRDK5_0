
module
{

    namespace Elastos{
    namespace Droid{
    namespace NFC{

    /**
     * @Involve
     * interace IParcelable
     */
    interface INdefMessage {
        GetRecords(
            [out] ArrayOf<INdefRecord*>* res);

        GetByteArrayLength(
            [out] Int32* len);

        byte[] toByteArray(
            [out] ArrayOf<Byte>* ba);

    }

    }// namespace NFC
    }// namespace Droid
    }// namespace Elastos
}