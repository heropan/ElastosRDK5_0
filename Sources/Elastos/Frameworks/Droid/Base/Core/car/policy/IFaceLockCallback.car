
module
{
    namespace Elastos {
    namespace Droid {
    namespace Internal {
    namespace Policy {

    interface IFaceLockCallback {
        Unlock();

        Cancel();

        ReportFailedAttempt();

        PokeWakelock(
            [in] Int32 millis);
    }

    } // Policy
    } // Internal
    } // Droid
    } // Elastos
}
