
module
{
    namespace Elastos {
    namespace Droid {
    namespace Utility {

    interface IPoolable {
        SetNextPoolable(
            [in] IPoolable* element);

        GetNextPoolable(
            [out] IPoolable** element);

        IsPooled(
            [out] Boolean* result);

        SetPooled(
            [in] Boolean isPooled);
    }

    } // namespace Utility
    } // namepsace Droid
    } // namespace Elastos
}
