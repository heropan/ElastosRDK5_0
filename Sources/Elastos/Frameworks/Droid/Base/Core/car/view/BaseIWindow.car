
module
{
    namespace Elastos {
    namespace Droid {
    namespace View {

    /**
     * @Involve
     * interface IIWindow
     */
    [deprecated]
    interface IBaseIWindow {
        SetSession(
            [in] IWindowSession* session);

        SetSeq(
            [in] Int32 seq);

        GetSeq(
            [out] Int32* seq);
    }

    } // namespace View
    } // namepsace Droid
    } // namespace Elastos
}
