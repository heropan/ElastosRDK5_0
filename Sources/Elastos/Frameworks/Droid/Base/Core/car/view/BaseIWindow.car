
module
{
    namespace Elastos {
    namespace Droid {
    namespace View {

    [deprecated]
    interface IBaseIWindow extends IIWindow {
        SetSession(
            [in] IWindowSession* session);

        SetSeq(
            [in] Int32 seq);

        GetSeq(
            [out] Int32* seq);
    }

    } // namespace View
    } // namepsace Droid
    } // namespace Elastos
}
