
module
{
    interface Elastos.Droid.Content.Res.ICompatibilityInfo;

    namespace Elastos {
    namespace Droid {
    namespace View {

    interface ICompatibilityInfoHolder {
        Set(
            [in] ICompatibilityInfo* compatInfo);

        Get(
            [out] ICompatibilityInfo** compatInfo);

        GetIfNeeded(
            [out] ICompatibilityInfo** compatInfo);
    }

    } // namespace View
    } // namepsace Droid
    } // namespace Elastos
}
