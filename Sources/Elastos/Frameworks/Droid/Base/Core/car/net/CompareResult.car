
module
{

    using interface Elastos.Utility.IList;

    namespace Elastos {
    namespace Droid {
    namespace Net {

    /**
     * @Involved
     * none super interface
     */
    interface ICompareResult {
        GetRemoved(
            [out] IList** result);

        GetAdded(
            [out] IList** result);
    }

    } // namespace Net
    } // namepsace Droid
    } // namespace Elastos
}
