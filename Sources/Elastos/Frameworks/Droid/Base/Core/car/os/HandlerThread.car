module
{
    namespace Elastos {
    namespace Droid {
    namespace Os {

    interface IHandlerThread extends Elastos.Core.Threading.IThread {
        GetLooper(
            [out] ILooper** looper);

        Quit(
            [out] Boolean* result);

        GetThreadId(
            [out] Int32* tid);
    }

    } // namespace Os
    } // namespace Droid
    } // namespace Elastos
}
