module
{
    namespace Elastos {
    namespace Droid {
    namespace Net {
    namespace Http {

    interface ILoggingEventHandler {
        requestSent();

        LocationChanged(
            [in] String newLocation,
            [in] Boolean permanent);
    }

    } // namespace Http
    } // namespace Net
    } // namespace Droid
    } // namespace Elastos
}
