module
{
    namespace Elastos {
    namespace Droid {
    namespace Speech {
    namespace Tts {

    [oneway]
    interface IITextToSpeechCallback {
        OnStart(
            [in] String utteranceId);

        OnSuccess(
            [in] String utteranceId);

        OnStop(
            [in] String utteranceId);
    }

    } // namespace Tts
    } // namespace Speech
    } // namespace Droid
    } // namespace Elastos

}
