
TARGET_NAME= content
TARGET_TYPE= lib

include $(MAKEDIR)/../../../../../../../../sources.inc

INCLUDES += ../../../
INCLUDES += $(MAKEDIR)/../../../../../../inc/

C_DEFINES += -DDROID_CORE

LIBRARIES += app/app.lib
LIBRARIES += browser/browser.lib
LIBRARIES += common/common.lib