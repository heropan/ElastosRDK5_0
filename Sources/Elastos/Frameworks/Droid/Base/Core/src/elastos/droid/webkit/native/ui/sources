TARGET_NAME = ui
TARGET_TYPE = lib

include $(MAKEDIR)/../../../../../../../../sources.inc

INCLUDES += ../../../
INCLUDES += $(MAKEDIR)/../../../../../../inc/

C_DEFINES += -DDROID_CORE

#SOURCES = ColorPickerSimple.cpp                    # cannot compile
SOURCES += DropdownDividerDrawable.cpp
SOURCES += VSyncMonitor.cpp
SOURCES += ColorSuggestion.cpp
#SOURCES += DropdownPopupWindow.cpp                 # cannot compile
SOURCES += UiUtils.cpp
SOURCES += ColorPickerAdvancedComponent.cpp
#SOURCES += DropdownAdapter.cpp                     # cannot compile
SOURCES += ColorSuggestionListAdapter.cpp
#SOURCES += ColorPickerMoreButton.cpp               # cannot compile
#SOURCES += ColorPickerAdvanced.cpp                 # cannot compile
#SOURCES += ColorPickerDialog.cpp                   # cannot compile

LIBRARIES += autofill/autofill.lib
LIBRARIES += base/base.lib
LIBRARIES += gfx/gfx.lib
LIBRARIES += gl/gl.lib
LIBRARIES += interpolators/interpolators.lib
