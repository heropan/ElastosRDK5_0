TARGET_NAME= sax
TARGET_TYPE= lib

include $(MAKEDIR)/../../../../sources.inc

INCLUDES += $(MAKEDIR)/../../inc
INCLUDES += $(MAKEDIR)/../../inc/sax
INCLUDES += ../

C_DEFINES += -DDROID_CORE

SOURCES = CChildren.cpp
SOURCES += Element.cpp
SOURCES += CElement.cpp
SOURCES += CRootElement.cpp