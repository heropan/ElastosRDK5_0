
module
{
    namespace Elastos {
    namespace Droid {
    namespace Server {

    interface ISystemServer {
         Init1(
            [in] ArrayOf<String> args);

         Init2();

         Main(
            [in] ArrayOf<String> args);
    }

    } // namespace Server
    } // namespace Droid
    } // namespace Elastos
}
