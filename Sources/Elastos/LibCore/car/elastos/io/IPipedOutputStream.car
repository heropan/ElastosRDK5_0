module
{

    namespace Elastos {
    namespace IO {

    interface IPipedOutputStream {
        Connect(
            [in] IPipedInputStream* ipis);
    }

    } // namespace IO
    } // namespace Elastos
}