module
{
    namespace Elastos {
    namespace IO {

        interface IPipedWriter {
            Connect(
                [in] IPipedReader* reader);
        }

    } // namespace IO
    } // namespace Elastos
}