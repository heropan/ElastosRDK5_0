
module
{
    // using interface Elastos.Security.IBasicPermission;
    // using interface Elastos.Security.IPermission;

    namespace Elastos {
    namespace Net {

    interface INetPermission { // extends IBasicPermission{
        GetActions(
            [out] String* str);

        // Implies(
        //     [in] IPermission* permission,
        //     [out] Boolean* value);
    }

    } // namespace Net
    } // namespace Elastos
}