module
{

    namespace Elastos {
    namespace HelloCar {

        interface IAnimal {
            SetName(
                [in] String name);

            GetName(
                [out] String* name);

            SetAge(
                [in] Int32 age);

            GetAge(
                [out] Int32* age);

            CanFly(
                [out] Boolean* canFly);
        }

        interface IAnimalHelper {
            CanFly(
                [in] IAnimal* animal,
                [out] Boolean* canFly);
        }

    } // HelloCar
    } // Elastos
}