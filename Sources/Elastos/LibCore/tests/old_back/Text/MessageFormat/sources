
TARGET_NAME = TestMessageFormat
TARGET_TYPE = ecx

IMPORTS = Elastos.CoreLibrary.eco
IMPORTS += Elastos.CoreLibrary.eco

INCLUDES += $(MAKEDIR)/../../../inc/Elastos/common;
INCLUDES += $(MAKEDIR)/../../../inc/Elastos/Text;


SOURCES = test.cpp
SOURCES += main.cpp

ELASTOS_LIBS = $(XDK_USER_LIB)/Elastos.Runtime.lib
ifeq "$(XDK_TARGET_PLATFORM)" "win32"
ELASTOS_LIBS += $(XDK_LIB_PATH)/Elastos.Runtime.lib
else
ELASTOS_LIBS += $(XDK_TARGETS)/Elastos.Runtime.eco
ELASTOS_LIBS += $(XDK_TARGETS)/Elastos.CoreLibrary.eco
LIBRARIES += -lstdc++ -lm -ldl -llog -lz -lcutils -lutils -lbinder -licuuc -licui18n
endif

